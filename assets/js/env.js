/* Placeholder env config — sobrescrito en build time por build/inject-env.js
 * Cuando esto está vacío, la app cae a las keys hardcoded de api.js (demo) o
 * a las que el usuario haya seteado manualmente en localStorage.
 */
window.__BS_CONFIG = window.__BS_CONFIG || {};
